# Generated by scripts/render.py and scripts/verify.py. Never hand-merge these:
# for a generated file the three-way merge result is never what you want, and
# re-running the generator is. A bad rebase once put conflict markers straight
# into the published SVGs.
assets/*.svg -merge
data/status.json -merge
